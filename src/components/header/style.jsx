import styled from 'styled-components'

export const HeaderStyle = styled.header`
    display: flex;
    padding: 10px;

    nav ul{
        display: flex;
        gap: 20px;
        list-style: none;
    }

    nav ul li a{
        text-decoration: none;
        color: black;
    }
`